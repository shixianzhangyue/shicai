/**
 * Placeholder hook for pipeline data.
 * Full implementation will be added in Step 10-11.
 */
export function usePipeline() {
  return {
    stages: [],
    pipeline: [],
    isLoading: false,
    error: null,
  };
}
